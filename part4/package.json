{
  "dependencies": {
    "bcrypt": "^5.0.1",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.2",
    "jsonwebtoken": "^8.5.1",
    "mongodb": "^4.3.0",
    "mongoose": "^6.1.4",
    "mongoose-unique-validator": "^3.0.0"
  },
  "scripts": {
    "test": "cross-env NODE_ENV=test2 jest --verbose",
    "dev": "cross-env NODE_ENV=test nodemon index.js",
    "start:test": "cross-env NODE_ENV=test2 node index.js"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "jest": "^27.4.5",
    "nodemon": "^2.0.15",
    "supertest": "^6.1.6"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
